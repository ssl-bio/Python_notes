:get-tables:
#+begin_src python -i :tangle "Ch-6_notes.py"
# Working data frame
web_address = 'https://www.w3schools.com/python/python_regex.asp'
out_file = 'regex.org'
regex_tb = pd.read_html(web_address)
with open(out_file, 'w') as fh:
    fh.write('Regular expressions')
    for i in range(len(regex_tb)):
        itb=regex_tb[i].to_markdown()
        print(itb, file=fh)
        print('', file=fh)
#+end_src
#+begin_src console -i

#+end_src
:END:        
